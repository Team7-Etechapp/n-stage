pipeline {
    agent any
    stages {
        stage ('1-Sherack') {
            steps {
                sh 'ps -ef'
                sh 'sudo systemctl status jenkins'
            }
        }
        stage ('2-John') {
            steps {
                sh 'ps -ef' 
                sh 'sudo systemctl status jenkins'
            }
        }
        stage ('3-Judith') {
            steps {
                sh 'ps -ef' 
                sh 'sudo systemctl status jenkins'
            }
        }
    }
}